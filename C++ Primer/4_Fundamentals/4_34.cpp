#include <iostream>

int main()
{ 
		bool  flag;   char           cval;
		short sval;   unsigned short usval;
		int   ival;   unsigned int   uival;
		long  lval;   unsigned long  ulval;
		float fval;   double		 dval;

		if (fval); // float to bool
		dval = fval + ival; // before +, int to float, then, float to double
		dval + ival * cval; // before *, char to int, before +, int to double
		return(0);
}
