/*
 * If that parameter were not a reference, then the call would never succeed
 * to call the copy constructor, we'd need to use the copy constructor to copy
 * the argument, but to copy argument, we'd need to call copt constructor, and so indefinitely;
 */
