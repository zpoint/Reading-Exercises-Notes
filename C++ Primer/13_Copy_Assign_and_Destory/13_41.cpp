/*
 * add first free and return the privious value
 * if use prefix, it will return the added value, which will leave the origin
 * first_free poisition remain unconstruct state
 *
 * When access to this memory, the result is undefined
 */
