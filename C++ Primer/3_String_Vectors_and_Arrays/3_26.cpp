/*
 * Because index 0 start in beg, beg is an iterator item
 * beg + index result in an iterator
 * index is an int
 * (beg + end) beg is an iterator, end is an iterator, iterator not support add operator
 */
