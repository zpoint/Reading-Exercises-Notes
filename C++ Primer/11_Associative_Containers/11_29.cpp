/*
 * lower_bound return an iterator to the first element with key not less than k
 * upper_bound return an iterator to the first element with key grater than k
 * ewual_range returns a pair of c.end()
 * /
